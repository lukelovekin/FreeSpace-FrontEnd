import React from 'react'

// this component shows up if no other component matches any URLs
export default function NoMatch() {
    return (
        <div>
            <p>No Match</p>
        </div>
    )
}
